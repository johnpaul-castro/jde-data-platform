name: 'jde'
version: '1.0.0'
config-version: 2

profile: 'jde'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  jde:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
